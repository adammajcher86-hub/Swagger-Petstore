
for f in tmpl*.cer; do
  echo "=== $f ==="
  openssl x509 -in "$f" -noout -subject -issuer 2>/dev/null || echo "INVALID or DER format"
done
curl -v https://registry.npmjs.org 2>&1 | grep -E "issuer|subject|SSL"
