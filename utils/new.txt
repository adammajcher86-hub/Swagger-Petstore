console.log('[openSuborder] current page URL:', page.url());
console.log('[openSuborder] current page title:', await page.title());


const allPages = this.browserContext.pages();
console.log('[openSuborder] all open pages count:', allPages.length);
for (const p of allPages) {
  console.log(' -', p.url());
}
