// Ile jest takich elementów?
const all = document.querySelectorAll('scale-text-field[name="sno"]');
console.log('Liczba elementów:', all.length);

all.forEach((el, i) => {
  console.log(`--- Element ${i} ---`);
  console.log('HTML:', el.outerHTML.slice(0, 150));
  console.log('Shadow root:', el.shadowRoot);
  console.log('Ma data-original:', el.hasAttribute('data-original'));
});

console.log('Czy zdefiniowany:', customElements.get('scale-text-field'));
