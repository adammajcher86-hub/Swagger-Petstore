// Check if dropdown exists
document.querySelector('#erfcLetterTemplate')
// Check if it's inside shadow DOM
document.querySelector('scale-dropdown-select[id="erfcLetterTemplate"]')
// Check all dropdowns on page
document.querySelectorAll('select, scale-dropdown-select, [role="combobox"]')
  .forEach(el => console.log(el.id, el.tagName, el.getAttribute('label')));
